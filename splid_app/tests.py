from django.test import TestCase
'''from .models import expence
from django.db.models import Sum

# Create your tests here.
grand_total = expence.objects.aggregate(total_expense=Sum('expenseAmount'))['total_expense']'''